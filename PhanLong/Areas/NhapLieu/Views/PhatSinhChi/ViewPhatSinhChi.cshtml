@model IEnumerable<PhanLong.EF.PhatSinhChiThu>
@using PhanLong.DAO
@using PhanLong.EF
@{
    PhanLongDBContext db = new PhanLongDBContext();
}

<div class="card-header">
    <h3 class="card-title">
        Phát Sinh Chi
    </h3>
</div>
<div class="card-body">
    <table class="table table-bordered table-hover table-striped" id="dataTable" width="100%">
        <thead>
            <tr>
                <th width="1%"><input type='checkbox' id='check' value='' /></th>
                <th>Ngày chi</th>
                <th>Người chi</th>
                <th>Nhân viên</th>
                <th>HTTT</th>
                <th>Khách Hàng</th>
                <th>Nội dung</th>
                <th>Xe</th>
                <th>Mooc</th>
                <th>Tên phí</th>
                <th>Số Bill</th>
                <th>Số hoá đơn</th>
                <th>Tiền Chi (VNĐ)</th>
                <th>Ghi Chú</th>
            </tr>

        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                var ct = db.CTChiThus.Where(x => (x.DMPhi.LoaiPhi == 1 || x.DMPhi.LoaiPhi == 3) && x.PhatSinhChiThu == item.Id).FirstOrDefault();
                if ((ct != null ? item.Id == ct.PhatSinhChiThu : false))
                {
                    var model = new CTChiDao().ListAll(item.Id);
                    <tr id="row_@item.Id">

                        <td><input type='checkbox' name='chkId[]' id='checkbox' value="@item.Id" /></td>
                        <td>@(item.Ngay != null? item.Ngay.ToShortDateString(): null)</td>
                        <td title="@(item.NguoiChiThu != null ? item.DMNhanVien.TenNV : null)">@(item.NguoiChiThu != null ? item.DMNhanVien.MaNV : null)</td>
                        <td title="@(item.NguoiNhan != null ? item.DMKhachHang1.TenCongTy : null)">@(item.NguoiNhan != null ? item.DMKhachHang1.MaKH : null)</td>
                        <td title="@(item.HTTT != null ? item.HinhThucTT.MoTa : null)">@(item.HTTT != null ? item.HinhThucTT.MoTa : null)</td>
                        <td title="@(item.KhachHang != null ? item.DMKhachHang.TenCongTy : null)">@(item.KhachHang != null ? item.DMKhachHang.MaKH : null)</td>
                        <td style="text-align:left">@(item.CTChiThus != null ? item.CTChiThus.FirstOrDefault().NoiDung : null)</td>
                        <td title="@(item.Xe != null ? item.DMXe.BienSo : null)">@(item.Xe != null ? item.DMXe.MaXe : null)</td>
                        <td title="@(item.Mooc != null ? item.DMMooc.BienSo : null)">@(item.Mooc != null ? item.DMMooc.MaMooc : null)</td>

                        @{var maphi = item.CTChiThus.Where(x => x.PhatSinhChiThu == item.Id).FirstOrDefault();}
                        <td title="@(maphi != null ? maphi.DMPhi.TenPhi : null)"> @(maphi != null ? maphi.DMPhi.MaPhi : null)</td>
                        @if (item.Bill != null)
                        {
                            <td>
                                @Html.ActionLink(item.DMBill.MaBill, "CTChi", new { id = item.Id })
                            </td>
                        }
                        else
                        {
                            <td></td>

                        }
                        <td>@item.SoHD</td>
                        <td style="text-align: right">
                            @{ var a = 0;
                                var tongtien = 0;
                                foreach (var tien in item.CTChiThus)
                                {
                                    tongtien = Convert.ToInt32(tien.DonGia) * tien.SoLuong;
                                    a += tongtien;
                                }
                            }@String.Format("{0:0,0}", (a))
                        </td>
                        <td>@item.GhiChu</td>
                    </tr>
                }
            }
        </tbody>
    </table>
    <div class="margin">
        <div class="btn-group">
            <button type="button" class="btn btn-primary dropdown-toggle dropdown-icon" data-toggle="dropdown">
                Hành động
                <span class="sr-only">Hành động</span>
            </button>
            <div class="dropdown-menu" role="menu">
                <input type="submit" value="Xóa" name="delete" id="delete" class="dropdown-item" />
                <input type="submit" value="Chỉnh sửa" name="update" id="update" class="dropdown-item" />
                <input type="submit" value="Chi tiết" name="chitiet" id="chitiet" class="dropdown-item" />
            </div>
        </div>
    </div>


</div>
